wsl -e bash -lc 'curl -L https://fly.io/install.sh | sh'

Execute wsl to enter Linux environment

Execute: nano ~/.bashrc

Enter the following and CTRL-X. Enter Y to save.
  export FLYCTL_INSTALL="/home/poonk/.fly"
  export PATH="$FLYCTL_INSTALL/bin:$PATH"

Exit Linux and open new terminal using WSL
Enter: which fly
 - should see this: /home/poonk/.fly/bin/fly

fly auth signup
fly launch --generate-name
 - Enter N as no need to tweak settings
 - Enter y to create dockerignore

fly apps list
 - To view list of apps

fly apps destroy <app-name>
 - For example: fly apps destroy holy-violet-6375